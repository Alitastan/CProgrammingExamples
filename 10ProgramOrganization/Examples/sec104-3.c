/*  
*   Suppose that a program has only one function (main) 
*   How many different variable named i could this program
*   contain ?
*/

// external variable i with static storage duration
int i;

int main (void)
{
    // local variable i with automatic storage duration
    int i;


    return 0;
}